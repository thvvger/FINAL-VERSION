{{ form_start(form) }}

<div class="row">
    <div class="col-md-12">

        <label for="pwd">Désignation:</label>
        {{ form_widget(form.designation) }}
    </div>
    <br><br>
    <div class="col-md-11 text-center mt-3 ">
        <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>

    </div>
</div>
{{ form_end(form) }}
