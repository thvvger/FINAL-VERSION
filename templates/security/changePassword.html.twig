{% extends "base.html.twig" %}

    
{% block  body %}
    {#{% include 'block/other_page_sub_header.html.twig' %}#}
    <div class="card">
        <div class="card-header">
            <h1>Modifier mon mot de passe</h1>
            <h6 class="card-subtitle text-muted"></h6>
        </div>
        <div class="card-body">
            <hr>
            {{ form_start(changePasswwordForm) }}
            {{ form_widget(changePasswwordForm) }}
                <div class="text-center m-3">

                    <button class="btn btn-info">{{ button_label|default('Valider') }}</button>
                </div>
            {{ form_end(changePasswwordForm) }}

        </div>
    </div>
{% endblock %}