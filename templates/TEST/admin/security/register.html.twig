{% extends 'base.html.twig' %}

{#{% form_theme form 'bootstrap_4_horizontal_layout.html.twig' %}#}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">

                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-success" class="btn btn-success"> Creer comptebbb<span class="fa fa-lock" ></span></button>
                {{ form_end(form) }}

            </div>
        </div>
    </div>
{% endblock %}
